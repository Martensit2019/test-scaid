# .

Современное веб-приложение на Vue 3 (Composition API) + TypeScript для отображения, фильтрации, сортировки и управления списком пользователей с возможностью загрузки фотографий.

## 🌐 Демонстрация

**Живое демо:** [https://test-scaid.vercel.app/](https://test-scaid.vercel.app/)

## 🚀 Особенности

- **Vue 3 Composition API** - современный подход к разработке компонентов
- **TypeScript** - строгая типизация для надежного кода
- **Pinia** - state management для централизованного управления данными
- **SCSS** - мощные возможности стилизации
- **Responsive Design** - адаптивный дизайн для всех устройств
- **Фильтрация и сортировка** - множественная фильтрация по имени, возрасту, email
- **Загрузка фотографий** - валидация размера и типа файлов
- **Оптимизация производительности** - виртуализация списков, debounce
- **Toast-уведомления** - элегантная система уведомлений

## 📋 Требования

- Node.js версии `^20.19.0` или `>=22.12.0`
- npm или yarn

## 🛠️ Установка и запуск

### Установка зависимостей

```sh
npm install
```

### Запуск в режиме разработки

```sh
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173/`

### Сборка для продакшена

```sh
npm run build
```

### Предварительный просмотр production-сборки

```sh
npm run preview
```

### Проверка типов TypeScript

```sh
npm run type-check
```

### Линтинг и автоисправление

```sh
npm run lint
```

### Форматирование кода

```sh
npm run format
```

## 📁 Структура проекта

```
src/
├── components/               # Vue компоненты
│   ├── UserList/            # Компоненты списка пользователей
│   │   ├── UserList.vue     # Основной компонент списка
│   │   ├── UserListFilters.vue  # Фильтры и поиск
│   │   └── UserCard.vue     # Карточка пользователя
│   ├── UserPhoto/           # Компоненты работы с фото
│   │   ├── UserPhotoUpload.vue    # Загрузка фото
│   │   └── UserPhotoDisplay.vue   # Отображение фото
│   ├── UI/                  # Переиспользуемые UI компоненты
│   │   ├── Button.vue
│   │   ├── Input.vue
│   │   ├── Select.vue
│   │   ├── Checkbox.vue
│   │   └── FileInput.vue
│   ├── ErrorBoundary.vue    # Обработка ошибок
│   └── ToastContainer.vue   # Система уведомлений
├── composables/             # Переиспользуемая бизнес-логика
│   ├── useUsers.ts          # Работа с пользователями
│   ├── useUserFilters.ts    # Логика фильтрации
│   ├── useUserSort.ts       # Логика сортировки
│   ├── usePhotoUpload.ts    # Загрузка фотографий
│   └── useToast.ts          # Управление уведомлениями
├── stores/                  # Pinia stores
│   └── userStore.ts         # Хранилище пользователей
├── types/                   # TypeScript типы
│   └── user.ts              # Интерфейс User
├── utils/                   # Вспомогательные функции
│   ├── userHelpers.ts       # Хелперы для пользователей
│   └── photoHelpers.ts      # Хелперы для фото
├── mocks/                   # Моковые данные
│   └── users.ts             # Тестовые пользователи
├── assets/                  # Статические ресурсы
│   └── scss/                # Стили
│       ├── main.scss        # Главный файл стилей
│       ├── _variables.scss  # Переменные
│       ├── _mixins.scss     # Миксины
│       └── _responsive.scss # Адаптивность
├── router/                  # Vue Router
│   └── index.ts             # Конфигурация роутов
├── views/                   # Страницы приложения
│   └── UsersPage.vue        # Страница пользователей
├── App.vue                  # Корневой компонент
└── main.ts                  # Точка входа
```

## 🎯 Использованные принципы и паттерны

### SOLID принципы

#### Single Responsibility Principle (SRP)
- Каждый компонент отвечает за одну задачу
- Типы вынесены в отдельные файлы (`types/`)
- Утилиты разделены по назначению (`utils/`)
- Бизнес-логика изолирована в composables

#### Open/Closed Principle (OCP)
- Компоненты открыты для расширения через props и slots
- Composables легко расширяются новой функциональностью
- Store расширяется новыми actions/getters без изменения существующих

#### Liskov Substitution Principle (LSP)
- UI компоненты взаимозаменяемы благодаря единому интерфейсу
- Компоненты наследуют общие паттерны и поведение

#### Interface Segregation Principle (ISP)
- Компоненты получают только необходимые props
- Интерфейсы разделены по функциональности
- Не заставляем компоненты зависеть от неиспользуемых данных

#### Dependency Inversion Principle (DIP)
- Компоненты зависят от абстракций (composables), а не конкретных реализаций
- Store скрывает детали реализации за публичным API
- Логика отделена от представления

### DRY (Don't Repeat Yourself)

- **Composables** - переиспользуемая логика фильтрации, сортировки, работы с фото
- **UI компоненты** - общие элементы интерфейса (Button, Input, Select и т.д.)
- **Utils функции** - общие утилиты для работы с данными
- **SCSS миксины** - переиспользуемые стили
- **Centralized state** - единое хранилище данных в Pinia

### Другие паттерны

- **Composition API** - композиция функциональности вместо наследования
- **Single Source of Truth** - Pinia store как единственный источник данных
- **Reactive State Management** - реактивное обновление UI
- **Component Composition** - сложные компоненты из простых
- **Props Down, Events Up** - однонаправленный поток данных
- **Virtual Scrolling** - оптимизация отрисовки больших списков
- **Debouncing** - оптимизация частых операций (поиск, фильтры)
- **Error Boundaries** - изолированная обработка ошибок
- **Type Safety** - строгая типизация на всех уровнях

## 🎨 Стилизация

- **SCSS** с модульной структурой
- **CSS переменные** для темизации
- **BEM-подобная** методология именования классов
- **Responsive breakpoints** для адаптивности
- **Mixins** для переиспользуемых стилей

## 🔧 Функциональность

### Управление пользователями
- Отображение списка пользователей в виде карточек
- Виртуализация для оптимизации больших списков
- Адаптивная сетка (1-4 колонки в зависимости от экрана)

### Фильтрация
- Поиск по имени, фамилии, email (с debounce)
- Фильтр по диапазону возраста (от-до)
- Фильтр по наличию фотографии
- Множественная фильтрация с логикой AND
- Кнопка сброса всех фильтров

### Сортировка
- По имени (A-Z, Z-A)
- По фамилии (A-Z, Z-A)
- По возрасту (возрастание, убывание)
- Переключение направления одним кликом

### Загрузка фотографий
- Drag & Drop интерфейс
- Выбор файла через диалог
- Валидация типа файла (JPEG, PNG, GIF, WEBP)
- Валидация размера (макс. 5 МБ)
- Предпросмотр загруженной фотографии
- Toast-уведомления об успехе/ошибке

### Уведомления
- Система toast-уведомлений
- Автоматическое скрытие через 3 секунды
- Разные типы (success, error, info)
- Анимированное появление/исчезновение

## 📊 Производительность

- **Virtual Scrolling** - рендер только видимых элементов
- **Debounce** - оптимизация поиска (300ms задержка)
- **Computed Properties** - кэширование вычислений
- **Lazy Loading** - отложенная загрузка компонентов
- **Code Splitting** - разделение бандла
- **Tree Shaking** - исключение неиспользуемого кода

## 🧪 Разработка

### Рекомендуемая IDE

[VS Code](https://code.visualstudio.com/) + [Vue (Official)](https://marketplace.visualstudio.com/items?itemName=Vue.volar) (отключите Vetur)

### Рекомендуемые расширения браузера

**Chromium (Chrome, Edge, Brave):**
- [Vue.js devtools](https://chromewebstore.google.com/detail/vuejs-devtools/nhdogjmejiglipccpnnnanhbledajbpd)
- [Custom Object Formatter](http://bit.ly/object-formatters)

**Firefox:**
- [Vue.js devtools](https://addons.mozilla.org/en-US/firefox/addon/vue-js-devtools/)
- [Custom Object Formatter](https://fxdx.dev/firefox-devtools-custom-object-formatters/)

### Технологический стек

- **Vue 3.5** - прогрессивный JavaScript фреймворк
- **TypeScript 5.9** - типизированный JavaScript
- **Pinia 3.0** - state management
- **Vue Router 4.6** - маршрутизация
- **Vite 7.2** - быстрый build tool
- **SCSS/Sass** - CSS препроцессор
- **ESLint** - линтер кода
- **Prettier** - форматтер кода

## 📝 Дополнительная информация

- **Конфигурация Vite**: [Vite Configuration Reference](https://vite.dev/config/)
- **TypeScript поддержка для .vue**: Используется `vue-tsc` вместо `tsc` для проверки типов
- **Поддержка типов в редакторе**: Требуется [Volar](https://marketplace.visualstudio.com/items?itemName=Vue.volar)

## 📄 Лицензия

Private project

## 👨‍💻 Автор

Создано как тестовое задание
